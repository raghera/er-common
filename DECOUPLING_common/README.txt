to generate source files, just type ant

By default it will only update files whose definition has changed in the xsd

to force regeneration of all classes, you can touch the common/pa_common.xsd file
or there's probably a flag to xjc


